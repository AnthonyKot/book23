# Brief — Chapter 11, "Where Every Route Must Pass" (payoff; no incident)

For Lane A, one invocation of `briefs/LANE-A-BATCH-2.md` after Chapter 09's integration:
`docs/HANDOVER-2.md` should list Chapter 11 as the next incomplete chapter. Two placeholders in
`chapters/11-where-every-route-must-pass.md`; no claims file (no incident); no new canonical value.

## 1. `{{excerpt:ch11-request-path}}` — the finished composition, readable

The chapter prints the fixed final build's middleware chain and explains it top-down. Today that
chain is spread through `buildAppWithSettings` in `service/app.go`. Refactor **only the final
stage's fixed composition** (the `chapter10` stage, which is also `NewChapter9App(Fixed)`'s route
and host policy) into one function of at most ~20 lines whose body is the wrapping, in this
order from the mux outward, and mark it:

```
mux
→ chapter09LookupGateway(Fixed, limits, …)      // ch. 4 budget, keyed on tenant+route
→ chapter05Authorization(Fixed, mux, routes, …) // ch. 1 scope (LoadInvoiceFor) then ch. 5 role
→ chapter08ErrorWriter(settings, store, …)      // ch. 8 opaque 404 body
→ chapter02Identity(Fixed, sessions, …)         // ch. 2 session → principal
→ chapter09Gateway(Fixed, …)                    // ch. 9 host filter
```

Behaviour must not change: every existing test passes unchanged, and every existing marked
excerpt stays byte-identical. Name it so the prose's "reads bottom-up: the last line is the first
thing a request meets" is literally true of the printed block (assign `handler` line by line, or
nest the calls; either is fine, but the request order above must be recoverable by reading it).
If the refactor would force changes to other stages' composition, leave those stages as they are
and have the final stage call the new function.

## 2. `{{excerpt:ch11-every-route-every-caller}}` — the whole-table loop, in `service/ch11_test.go`

A test `TestChapter11EveryRouteEveryCaller` on `NewChapter10App(Fixed)`:

- Iterate `app.Routes()` (20 routes) × callers {anonymous, Alice, Ben, Dana} × one concrete
  request per route (fill `{id}` with 104 and then 205; `{quote}` with `q-771` after quoting;
  `{id}` on admin users with `alice`; bodies minimal and valid, e.g. `{"reference":"PO-9"}`,
  `{"display_name":"A"}`, webhook `{"url":"https://hooks.cedar.example/ledger"}` with a canned
  transport that resolves public). One request per cell; sequences are not this test's job.
- Assert, for every cell, the four properties the chapter states:
  1. **No cross-tenant data in any body**: Ben's bodies never contain `Cedar`, `LGR-`-numbers or
     names belonging to Cedar, `Alice`, `Dana`, or invoice 104's number; Alice's and Dana's never
     contain `Birch`, `Ben`, or invoice 205's number. (Use the fixture strings actually seeded.)
  2. **No internal field in any body**: neither `collections_note`/`CollectionsNote` nor
     `margin`/`Margin` appears in any response to anyone, Dana included.
  3. **Every 404 on `GET /v2/invoices/{id}` has the exact body `{"error":"not found"}`.**
  4. **Status matches the declaration**: anonymous on `User`/`TenantAdmin` → 401; a `user`-role
     caller on `TenantAdmin` → 403 when the target is in-tenant, 404 when the route names a
     foreign invoice/user/quote (scope before role); `Public` routes → not 401/403 for anonymous.
     Routes whose handler legitimately answers 400/404/409 for the minimal body (e.g. OTP verify
     without a challenge, confirm with no quote) are allowed those statuses; list each exemption
     in the test as a named case, not a blanket skip.
- The marked excerpt is the loop itself (~20 lines): routes × callers × request → the four
  property checks as helper calls. Helpers live below the marker.
- Add the test group(s) to `verify.sh`, plus excerpt-equality checks for both ch11 markers, and
  fill both placeholders in the chapter in the same form as the other chapters.

## 3. Exercise table (the chapter's A–H; assert in `TestChapter11ExerciseLayers`)

Fixed build only (the chapter has no vulnerable side). Expected status and, where observable, the
layer: A `GET /v2/health` anonymous → 200 `{"ok":true}`; B `GET /v2/admin/health` anonymous → 401
(Dana → 200, body has `version` only); C Alice `GET /v2/invoices/205` → 404 body exactly
`{"error":"not found"}`; D Alice `DELETE /v2/invoices/104` → 403, 104 still present; E Dana
`DELETE /v2/invoices/205` → 404, 205 still present; F Alice 31st `GET /v2/invoices?email=…` in a
minute → 429, and the 31st from a different `RemoteAddr` is also 429; G Alice
`GET ledger-staging.internal/v2/invoices/104` → 404, and the same on `api.ledger.example` → 200;
H Alice `POST /v2/webhooks/test` `{"url":"https://hooks.cedar.example/ledger"}` with the fake
resolver answering `169.254.169.254` → 400, no dial attempted.

Where a layer is not observable from the response alone, prove it the way the ch. 9 test does
(e.g. G: identity never ran, so a garbage token on the staging host gets the same 404).

## 4. Handover

Record in `docs/HANDOVER-2.md` any property in §2 that a cell cannot satisfy and why; do not
weaken the property to make it pass. Update `CONTEXT.md` §6 row 11 only by proposal in the
handover. Add a `site/catalog.mjs` row only if the author has not (they have). Do not push.
