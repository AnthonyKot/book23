# Book 23 review — 2026-09-23

Scope: Chapters 00–03, one chapter at a time. Findings were written before the edits described
under each chapter. This is an evidence and mechanism review by a model, not an independent
human reader's reaction.

## Chapter 00 — The Lookup That Never Asks

Mechanism: Coinbase's Retail Advanced Trading validation checked the named source account's
balance without checking that its asset matched the order book. Tree of Alpha's first test
changed `product_id` while leaving account IDs unchanged; the later SHIB test changed the source
account. This is an account/asset consistency failure, not cross-user BOLA.

Findings before edits:

- **FIX:** “Every check that ran, passed” is broader than the vendor record. The source names a
  balance check that passed and an asset/order-book match that was missing; it does not enumerate
  every check in the request path.
- **NOTE:** Claims 00-08 and 00-09 and the Tree of Alpha archive were added in the intervening
  commits. The two demonstrations are now correctly separated in the prose.
- **NOTE:** The opener's Ledger cast and fixed numbers establish a specific spine. Its hinge is
  “Which check did this request assume had already happened?”; the next door is the invoice
  lookup in Chapter 1. There is no exercise in the short opener.
- **NOTE:** The promise that every handler exists and has running tests is a final-book promise;
  it is not yet true for the exploratory Chapters 2 and 3. Recheck it at publication rather than
  weakening the intended product promise here.

Changed: `chapters/00-the-lookup-that-never-asks.md` narrows the assertion to the documented
balance check. The source-claim rows and archive were already present when this pass resumed.

Left for the author: verify the final-book promise about every handler and test before publication;
that cannot be proved while Chapters 2 and 3 remain prose drafts.

Verdict: **ready for the author's read** as an opener. Its most important remaining condition is
the final-book code/test promise, not the Coinbase mechanism, which is now source-accurate.

## Chapter 01 — Who's Asking

Mechanism: Peloton's `POST /stats/workouts/details` accepted manually changeable IDs; adding a
login requirement still let authenticated members see other members' data, including data from
private profiles. This supports API1 BOLA as an exposure case, without proving Peloton's server
code or the exact origin of every field in the disclosure.

Findings before edits:

- **BLOCK:** Exercise cases D (refund quote/confirm) and E (client-selected tenant list) have no
  handlers or both-mode tests in the pilot service. A/B/C are reasoned contrasts; the service
  tests cover the real invoice, PDF, and server-filtered list paths, not all five exercise
  variants. The chapter cannot yet meet the book's executable-exercise contract.
- **FIX:** The opening assigns the disclosure's full combined field list and a “leaderboard”
  purpose to this one endpoint. The source documents the request and private-profile response,
  but the list covers several reported API issues. Narrow the endpoint claim.
- **FIX:** The three printed Go blocks include `// ch01-*` labels absent from the marked source
  excerpts. Remove those labels so the blocks are byte-for-byte excerpt content.
- **FIX:** `currentUser` is a helper called inside the handler, not middleware in this fixture;
  the vulnerable handler discards the returned user. The prose should describe what runs.
- **FIX:** “IDs on every leaderboard,” the server's supposed assumption that displayed IDs were
  vetted, and Peloton's supposed reason for the partial fix go beyond the source. State observed
  behavior and mark the general client-trust pattern as a generalization.
- **FIX:** The Chapter 1 route-loop test is a static list. It does not itself catch a new v3
  route; a separate registered-route coverage assertion would be required.
- **NOTE:** The hinge is “which routes can reach this data, and does every one of them check?”
  Ledger's `/v1` and PDF reuse invoice 205 and the shared loader. B is the plausible safe decoy;
  B/E is the near-identical filter pair with opposite results. D adds a distinct two-step trap.
  A cold reader can predict B/E after tracing the tenant source, but D requires careful attention
  to which `invoice_id` reaches confirm.

Changed: `chapters/01-whos-asking.md` narrows the incident to the observed endpoint, removes
unsupported field and leaderboard claims, describes the actual login helper and discarded user,
separates Peloton's observed partial fix from a general server-side failure pattern, and states
what a route-coverage assertion would still need to do. Removed three artificial Go labels;
all three printed Go blocks now match their marked service excerpts exactly.

Validation: `go test ./...` from `service/` passed; `./verify.sh` passed its Chapter 1 and 9
groups, source-manifest checks and marker checks. It reports chapter/link checks pending because
the repo has no pilot HTML files. The script did not prove the unimplemented exercise paths.

Left for the author and later implementation: decide whether D's refund sequence and E's
client-selected tenant list should be built in the service and asserted in both modes or revised
to cases already executable. Also add a registered-route-to-test-coverage assertion before
claiming a future `/v3` cannot be missed. These are larger than a sentence-level prose repair.

Verdict: **not yet ready as an executable chapter**. The primary incident and prose mechanism
are now clean; the missing exercise handlers and both-mode assertions are the decisive block.

## Chapter 02 — One Key for Every Door

Mechanism: BrewDog's Android app carried a static bearer token shared across installations;
changing the customer ID appended to an endpoint URL returned another customer's data. The
credential authenticated possession of the shared key, not a person, so API2 is a defensible
primary class even though the observed result also involved missing object authorization.

Findings before edits:

- **BLOCK:** Chapter 2 has no service handlers, marked Go excerpts, or both-mode tests yet. Its
  A/C session-header pair is a sound near-identical contrast, but the D/E tenant-key invoice
  probe gives the same 200 invoice outcome in both rules; it does not visibly prove the claimed
  identity difference. The brief needs a person-scoped or identity-returning probe when built.
- **FIX:** `checks/claims/02.tsv` is absent. The archived Alan Monie disclosure supports the
  token, ID substitution, field list, version timeline, and limited claim about notification;
  create a locator-bearing ledger before treating those incident details as gated.
- **FIX:** An extracted app key proves possession of the key, not that a request came from a
  legitimate build of the app. The prose says otherwise twice.
- **FIX:** The article says Monie knew of no customer notification at publication, not that
  notification never occurred. Version 2.5.12 still had a static key that exposed discount codes;
  the article does not say it still exposed every earlier PII field.
- **FIX:** Ledger's `X-User` header is an invented analogy. BrewDog's published mechanism used a
  customer ID in the URL; the exercise answer must not equate them literally. A tenant admin like
  Dana is also not a platform-support impersonation role.
- **FIX:** The draft's “two hundred thousand phones” transfers a shareholder count to app
  installations without evidence. `/v1` continues serving until Chapter 9, so “being retired”
  needs tighter wording.
- **NOTE:** The chapter reuses `LoadInvoiceFor`, `/v1`, invoice 104/205 and Ben/Alice. Its hinge
  is “can a request become Alice without Alice's credential?” The decoy A and A/C pair make that
  question predictable; the D/E branch currently weakens the exercise by switching to a second
  actor type with no visible invoice difference.

Changed: created `checks/claims/02.tsv` with ten rows located in the archived primary disclosure.
In `chapters/02-one-key-for-every-door.md`, replaced the unsupported decompiler claim with what
the article says, qualified notification, distinguished key possession from app provenance,
corrected the revocation and shareholder-to-phone-count assertions, kept `/v1` serving until
Chapter 9, separated BrewDog's URL ID from Ledger's invented `X-User`, and removed Dana from a
platform-support role. The answer now specifies a person-scoped identity probe for D/E; an
invoice-only 200 would not show the difference.

Left for the author and implementation: choose and implement the person-scoped probe and include
it in the Chapter 2 case matrix; build marked handlers and both-mode tests for all exercise
cases. The current chapter's `{{excerpt:...}}` markers correctly remain placeholders.

Verdict: **source-gated but not yet executable**. The primary auth mechanism is sound; the most
important unresolved issue is that the proposed identity cases are not backed by service code
or tests.

## Chapter 03 — The Row You Didn't Mean to Send

Mechanism: DPD's unauthenticated map call revealed a postcode from a parcel code; that postcode
passed the tracking-page gate, after which the page's session exposed JSON containing recipient
contact and parcel details. The JSON is a useful property-exposure example, but the demonstrated
intrusion depends on the weak parcel-code/postcode gate. The record does not establish a separate
policy saying which fields an authorized recipient was permitted to receive, so API3 is an
illustration here rather than the incident's proven sole failure class.

Findings before edits:

- **BLOCK:** No Chapter 3 service handlers, marked excerpts, or both-mode tests exist. The
  `{{excerpt:...}}` placeholders are intentional under this pass. The exercise's B decoy and D/E
  typed-versus-map patch pair are good reasoning contrasts, but none of its six cases is yet
  executable. The author must also decide whether DPD's gate-dependent disclosure is strong
  enough to lead an API3 chapter.
- **FIX:** `checks/claims/03.tsv` is absent. The primary article supports the map call, postcode
  derivation, tracking gate, session JSON, dates and publication delay. Its embedded JSON image
  shows a contact name and mobile field but `email: null`; the draft's claim that the server sent
  a full name, email address and mobile number as populated values overstates this example.
- **FIX:** The article says both “within one week” and “within three weeks,” while its dated
  timeline records 2 September through confirmation of resolution on 6 October. Use the dated
  timeline and remove the unqualified duration and inferred lifetime of the exposure.
- **FIX:** “The caller was allowed to see the object” and “payload nobody had looked at” assert
  DPD policy and review history that the report does not prove. State the observed gate bypass
  and JSON contents, then identify Ledger's property policy separately.
- **FIX:** Chapter 1 left a shared loader for invoice-by-ID paths and a tenant-filtered list,
  not “one door” through which every route passes. Go's JSON decoder writes matching exported
  fields on a struct, not literally every arbitrary request key. The draft also asserts a test
  will fail for a proposed guard before the Chapter 3 tests exist.
- **NOTE:** The spine reuses Alice, invoice 104, `LoadInvoiceFor`, `/v1` PDF and the list route;
  the hinge is “which routes encode this type, and does each one encode a view?” The B decoy is
  predictable after the view rule, and D/E differs only by the decoded type. A cold Go reader can
  trace input to field change. The chapter adds property selection after Chapter 1's object
  selection rather than simply repeating the authorization lesson.

Changed: `chapters/03-the-row-you-didnt-mean-to-send.md` now uses the primary disclosure's
dated timeline, distinguishes the screenshot's populated contact name from its null email,
and treats DPD as an API3 illustration with a separately proven weak gate. It corrects the
Chapter 1 route description, the Go decoder claim, the premature test claims, and two places
where view use was described as universal. Created `checks/claims/03.tsv` with eight located
primary-source rows, archived the disclosure's JSON screenshot, and registered its hash in
`resources/incidents/SOURCES.tsv`. The incident and worked Ledger example remain in place.

Validation: `go test ./...` from `service/` passed. `./verify.sh` passed Chapter 1 and Chapter 9
tests, source-archive/link checks, and 11 chapter pages; it reports 31 code blocks pending.
Neither command proves Chapter 3's unbuilt cases. The Chapter 3 claims file has eight seven-column
rows with existing archives, and `git diff --check` passed.

Left for the author and implementation: decide whether the gate-dependent DPD disclosure is a
strong enough lead for API3 after this qualification; implement the five planned excerpts and
six exercise cases in both modes before calling the chapter executable. The current placeholders
are intentional under this review's scope.

Verdict: **not yet ready as an executable chapter**. Its Ledger property-level mechanism is
clear and source claims are now gated, but the Chapter 3 service and tests do not exist.

## Proposed changes left outside the sentence-level pass

- Chapter 01: build or replace the refund quote/confirm and client-selected tenant-list
  exercise paths; add both-mode assertions and a registered-route coverage assertion.
- Chapter 02: build its handlers, marked excerpts and both-mode exercise tests; choose a
  person-scoped route whose outcome distinguishes the D/E identity cases.
- Chapter 03: make the editorial incident/class-fit decision, then build the five planned
  excerpts and both-mode tests for its six exercise cases. A different lead incident would
  require an opening rewrite and new source gate, so this pass did not replace DPD.
